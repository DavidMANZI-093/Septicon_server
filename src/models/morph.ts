export interface pairs {
    count: number,
    label: "prs",
}

export interface pieces {
    count: number,
    label: "pcs",
}

export interface kilos {
    count: number,
    label: "kgs",
}

export interface litres {
    count: number,
    label: "ltr",
}

export interface meters {
    count: number,
    label: "mtr",
}

export interface packages {
    count: number,
    label: "pkg",
}